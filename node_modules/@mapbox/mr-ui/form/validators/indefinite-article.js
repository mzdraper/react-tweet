'use strict';

exports.__esModule = true;
exports.default = indefiniteArticle;
function indefiniteArticle(noun) {
  var article = /[aeiou]/i.test(noun.charAt(0)) ? 'an' : 'a';
  return article + ' ' + noun;
}