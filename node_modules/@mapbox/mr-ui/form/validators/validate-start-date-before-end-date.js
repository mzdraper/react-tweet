'use strict';

exports.__esModule = true;
exports.default = validateStartDateBeforeEndDate;

var _isEmptyControlValue = require('./is-empty-control-value');

var _isEmptyControlValue2 = _interopRequireDefault(_isEmptyControlValue);

var _validateDate = require('./validate-date');

var _validateDate2 = _interopRequireDefault(_validateDate);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function validateStartDateBeforeEndDate(_ref) {
  var date = _ref.date,
      endDate = _ref.endDate;

  var startDate = date;
  if ((0, _isEmptyControlValue2.default)(startDate)) {
    // if you have an end date, you must have a start date
    return 'Please select a start date';
  }
  if ((0, _isEmptyControlValue2.default)(endDate)) {
    // if you have an end date, you must have a start date
    return 'Please select an end date';
  }
  var startDateError = (0, _validateDate2.default)(startDate);
  if (startDateError) {
    return (0, _validateDate2.default)(startDate);
  }
  var endDateError = (0, _validateDate2.default)(endDate);
  if (endDateError) {
    return (0, _validateDate2.default)(endDate);
  }
  if (startDate.getTime() > endDate.getTime()) {
    return 'Please select a start date before your end date';
  }
  return '';
}